import { createComponent } from "@lit-labs/react"
import React from "react"
import { AccordionPattern } from "spacekit"

const SpaceAccordionPattern = createComponent({
    react: React,
    elementClass: AccordionPattern,
    tagName: "space-accordion-pattern",
})

export default SpaceAccordionPattern

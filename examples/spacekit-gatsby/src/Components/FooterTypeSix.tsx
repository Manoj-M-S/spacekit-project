import { createComponent } from '@lit-labs/react';
import React from 'react';
import { TypeSix } from 'spacekit';

const SpaceFooterTypeSix = createComponent({
  react: React,
  elementClass: TypeSix,
  tagName: 'space-footer-type-six',
});

export default SpaceFooterTypeSix;
